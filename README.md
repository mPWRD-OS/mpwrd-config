# mpwrd-config

MPWRD configuration tooling (Python-first, KISS).

## Quick start

- Install build dependencies and package using the Debian workflow.
- Run `mpwrd-config` for the TUI or `mpwrd-config-cli` for the CLI.
